import Foundation

enum AppStore {
    static let modules = FrontendMocks.modules
    static let inventory = FrontendMocks.inventory
    static let projects = FrontendMocks.projects
    static let posts = FrontendMocks.posts
    static let suggestions = FrontendMocks.suggestions
}
